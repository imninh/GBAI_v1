# Core
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
pydantic>=2.10.0
pydantic-settings>=2.7.0
python-dotenv>=1.0.0
pyvrp>=0.9.0


# AI / LangChain
langchain>=0.3.0
langchain-openai>=0.3.0
langgraph>=0.2.0

# Database
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0

# Xử lý ảnh — tước EXIF, nén, làm mờ khuôn mặt, băm tri giác (pHash)
pillow>=11.0.0
imagehash>=4.3.0
# Pinned below 5.0: OpenCV 5 removed CascadeClassifier and ships no bundled face model
opencv-python-headless>=4.10.0,<5.0.0

# Upload file cho FastAPI
python-multipart>=0.0.20

# Doc workbook GIS — scripts/nhap_cu_dan_gis.py dung de nhap va KHOI PHUC
# 600 tai khoan cu dan sau su co mat du lieu. Truoc day khong khai o dau,
# nen script chi chay duoc tren may da cai san — dung luc can khoi phuc
# thi no vang ModuleNotFoundError.
openpyxl>=3.1.0

# Xác thực — tự làm auth thay vì Supabase, xem ADR-0004
pyjwt>=2.10.0

# Tầng T0.5 — CLIP zero-shot.
#
# `onnxruntime` chạy bản CLIP đã nén int8 (chỉ nửa ảnh, ~85 MB) nên tầng này
# chạy được cả trên máy chủ 512 MB — xem ADR-0007. `torch` (1,19 GB) chỉ cần khi
# muốn chạy bản đầy đủ ở máy dev hoặc khi xuất model: requirements-local-model.txt.
onnxruntime>=1.19.0
numpy>=1.26.0

# Dev tools
ruff>=0.8.0
pytest>=8.0.0
pytest-asyncio>=0.24.0
httpx>=0.28.0
